# RiverTwin AI — Vite + React + Cesium scaffold

Quick scaffold for the hackathon project. Run:

```bash
npm install
npm run dev
```

This scaffold includes placeholder components for Cesium viewer, sidebar, rainfall slider, layer toggles, risk panel, an AI risk module, and a color scale utility.